{% extends "layout.html" %}
{% block body %}
  {% if session.logged_in %}
    <form action="{{ url_for('add_comment') }}" method=post class=add-comment>
      <dl>
        <dt>CommentID:
        <dd><input type=text size=16 name=CommentID>
	<dt>ParkID:
        <dd><input type=text size=16 name=ParkID>
        <dt>Content:
        <dd><textarea name=text rows=5 cols=40></textarea>
	<dt>Rating:
        <dd><input type=text size=16 name=Rating>
        <dd><input type=submit value=Share>
      </dl>
    </form>
  {% endif %}
  <ul class=parks>
  <h1>Comments (About Park)</h1>
  {% for comment in comments %}
    <li><h2>{{ comment.parkname}}</h2> <br> <i> <q>{{ comment.content|safe }} </q> </i> <br/>  <br><b> {{ comment.username|safe}}</b>  {{comment.postdate|safe}} <br/>
  {% else %}
    <li><em>Unbelievable.  No Comments so far</em>
  {% endfor %}
  </ul>
{% endblock %}       
