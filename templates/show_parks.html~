{% extends "layout.html" %}
{% block body %}
  <ul class=parks>
  {% for park in parks %}
    <li><h2>{{ park.parkname }}</h2>{{ park.parktype|safe }}
  {% else %}
    <li><em>Unbelievable.  No parks so far</em>
  {% endfor %}
  </ul>
{% endblock %}
